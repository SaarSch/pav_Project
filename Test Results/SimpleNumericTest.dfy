/** Automatically-generated code.
 */

/** Synthesized method.
 */
method simpleNumericTest(n : int?) returns (i0 : int?)  {
  // Initialize local variables to their default values.
  var x : int? := 0;
  i0  := 0;

  assert true;
  x := n;
  assert true;
  x := (x + 1);
  assert true;
  x := (x / 3);
  assert true;
  i0 := (n + x);
  return;
  return i0;
}