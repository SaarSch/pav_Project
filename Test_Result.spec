gcd(mut a:int, mut b:int) -> (res:int) {
  
  example {
    [a==3 && b==54]
    -> b = b - a; // [b==51]
    -> b = b - a; // [b==48]
    -> b = b - a; // [b==45]
    -> b = b - a; // [b==42]
    -> b = b - a; // [b==39]
    -> b = b - a; // [b==36]
    -> b = b - a; // [b==33]
    -> b = b - a; // [b==30]
    -> b = b - a; // [b==27]
    -> b = b - a; // [b==24]
    -> b = b - a; // [b==21]
    -> b = b - a; // [b==18]
    -> b = b - a; // [b==15]
    -> b = b - a; // [b==12]
    -> b = b - a; // [b==9]
    -> b = b - a; // [b==6]
    -> b = b - a; // [b==3]
    -> res = a; // [res==3]
  }
}
